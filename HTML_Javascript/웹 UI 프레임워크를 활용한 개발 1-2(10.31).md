# 10/31 기록
### this.byId()
- View에 정의된 컨트롤을 ID로 찾아서 반환
```java
let oTable = this.byId("idCustomers2");
oTable.addItem(new sap.m.ColumnListItem()); // 테이블에 행 추가 가능
```

### getSelectedItem()
- 리스트나 테이블에서 현재 선택된 항목을 가져오는 메서드
```java
onReadBooking: function(oEvent) {
      let table = this.byId("idCustomers2");  // this.byId 사용해서 Customers 테이블 가져오기
      let oRow = table.getSelectedItem();     // 현재 선택한 아이템 정보 들어옴   

      let oBindingContext = oRow.getBindingContext("customer");
      let sPath = oBindingContext.getPath();
      let oCustomer = this.getView().getModel("customer").getProperty(sPath);

      let oBookingModel = this.getView().getModel("booking");
      oBookingModel.setProperty("/result", oCustomer._Bookings);
  }
```

oEvent.getParameter("value")
this.byId("idCustomers2")
</br>

### oEvent.getParameter("value") 와 this.byId("idSomething")
- view.xml 파일
```html
<Input id="idInput" placeholder="이름 입력" change=".onInputChange" />
<Button text="이름 읽기 (byId)" press=".onReadInputValue" />
```
- controller.js 파일
```java
onInputChange: function (oEvent) {
    // 👉 방법 ① 이벤트로부터 값 가져오기
    let sValue = oEvent.getParameter("value");
    console.log("이벤트 방식:", sValue);
},

onReadInputValue: function () {
    // 👉 방법 ② 컨트롤 직접 접근해서 값 가져오기
    let sValue = this.byId("idInput").getValue();
    console.log("byId 방식:", sValue);
}
```
