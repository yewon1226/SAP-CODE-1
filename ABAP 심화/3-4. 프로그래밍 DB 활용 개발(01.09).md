### 1) EXIT
- ALV에서 수정 허용 여부나 처리 대상을 판단하려고, 실제 DB 필드와 별도로 플래그 필드를 구조에 추가해서 로직 제어함
- ALV에서 해당 컬럼을 사용자가 수정 가능하도록 설정하는 UI 플래그
```abap
gs_fcat-edit = 'X'.
```
```abap
MODULE user_command_0100 INPUT.
  CASE OK_CODE.
    WHEN 'EDIT'.
      PERFORM set_edit_rows.
  ENDCASE.
ENDMODULE.
```
